services:
  whisper:
    image: ghcr.io/ggml-org/whisper.cpp:main-cuda-${WHISPER_CPP_REF}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
