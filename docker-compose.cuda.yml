services:
  whisper:
    image: ghcr.io/ggml-org/whisper.cpp:main-cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
